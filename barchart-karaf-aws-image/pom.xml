<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.barchart.base</groupId>
		<artifactId>barchart-archon</artifactId>
		<version>2.3.2</version>
		<relativePath/>
	</parent>

	<groupId>com.barchart.karaf</groupId>
	<artifactId>barchart-karaf-aws-image</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>

		<projectJavaVersion>1.6</projectJavaVersion>

		<!-- location of build resources on local -->
		<publishSource>${basedir}/target/publish</publishSource>

		<!-- location of build resources on remote -->
		<publishTarget>/tmp/publish_${projectTimeFile}</publishTarget>

		<!-- path to install script -->
		<publishInstall>${publishTarget}/install.sh</publishInstall>

	</properties>

	<profiles>

		<profile>

			<id>image-build</id>

			<build>

				<plugins>

					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-resources-plugin</artifactId>
						<executions>
							<execution>
								<id>publish-prepare</id>
								<goals>
									<goal>copy-resources</goal>
								</goals>
								<phase>package</phase>
								<configuration>
									<resources>
										<resource>
											<directory>src/main/script</directory>
											<filtering>true</filtering>
										</resource>
										<resource>
											<directory>src/main/template</directory>
											<filtering>true</filtering>
										</resource>
									</resources>
									<outputDirectory>${publishSource}</outputDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<plugin>

						<groupId>com.carrotgarden.maven</groupId>
						<artifactId>carrot-maven-aws-plugin</artifactId>
						<version>1.1.2</version>

						<configuration>

							<!-- used by cloud formation & elastic compute goals -->
							<amazonRegion>${amazonRegion}</amazonRegion>

							<!-- used by cloud formation goals -->
							<stackName>${stackName}</stackName>
							<stackServerId>barchart-karaf-stack</stackServerId>
							<stackTemplateFile>${publishSource}/formation.template</stackTemplateFile>
							<stackPropertiesInputFile>${publishSource}/formation.properties</stackPropertiesInputFile>
							<stackInputParams>
								<KeyName>barchart-karaf</KeyName>
								<InstanceType>m1.small</InstanceType>
								<DNSZoneName>aws.barchart.com.</DNSZoneName>
								<DNSHostName>${DNSHostName}</DNSHostName>
							</stackInputParams>

							<!-- used by ssh goals -->
							<sshHost>${DNSHostName}</sshHost>
							<sshKeyFile>${user.home}/.ssh/barchart-karaf/barchart-karaf.pem</sshKeyFile>

							<!-- used by elastic compute goals -->
							<computeServerId>barchart-karaf-compute</computeServerId>
							<imageName>java-7-karaf_${projectTimeFile}</imageName>
							<imageDescription>java 7 with karaf (${projectTimeFile})</imageDescription>

						</configuration>

						<executions>

							<execution>
								<id>stack-create</id>
								<phase>package</phase>
								<goals>
									<goal>cloud-formation-create</goal>
								</goals>
							</execution>

							<execution>
								<id>script-publish</id>
								<phase>package</phase>
								<goals>
									<goal>secure-shell-publish</goal>
								</goals>
								<configuration>
									<sshSource>${publishSource}</sshSource>
									<sshTarget>${publishTarget}</sshTarget>
									<sshStatusSuccess>
										<status>-1</status>
									</sshStatusSuccess>
								</configuration>
							</execution>

							<execution>
								<id>script-execute</id>
								<phase>package</phase>
								<goals>
									<goal>secure-shell-execute</goal>
								</goals>
								<configuration>
									<sshCommand>sudo bash ${publishInstall}</sshCommand>
									<sshStatusSuccess>
										<status>0</status>
									</sshStatusSuccess>
								</configuration>
							</execution>

							<execution>
								<id>image-register</id>
								<phase>package</phase>
								<goals>
									<goal>elastic-compute-image-register</goal>
								</goals>
							</execution>

							<execution>
								<id>stack-delete</id>
								<phase>package</phase>
								<goals>
									<goal>cloud-formation-delete</goal>
								</goals>
							</execution>

						</executions>

					</plugin>

				</plugins>

			</build>

		</profile>

		<profile>
			<id>us-east-1</id>
			<properties>
				<amazonRegion>us-east-1</amazonRegion>
				<stackName>karaf-us-barchart-com</stackName>
				<DNSHostName>karaf-image.us.aws.barchart.com.</DNSHostName>
			</properties>
		</profile>

		<profile>
			<id>eu-west-1</id>
			<properties>
				<amazonRegion>eu-west-1</amazonRegion>
				<stackName>karaf-eu-barchart-com</stackName>
				<DNSHostName>karaf-image.eu.aws.barchart.com.</DNSHostName>
			</properties>
		</profile>

	</profiles>

</project>
